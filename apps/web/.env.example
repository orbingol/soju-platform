# Copy to .env for local Vite dev outside Docker (optional — compose sets these in the web service).
# Models / tutor prompt / TTS voice are loaded from the Soju backend: GET /v1/soju/client-config
PUBLIC_AI_ENABLED=true
PUBLIC_AI_BASE_URL=http://localhost:8080
PUBLIC_TTS_ENGINE=local
