``soju`` (unified entry)
========================

Optional single binary that mounts the same commands as subcommands
(``soju import …``, ``soju promote …``, ``soju registry``, …).

.. typer:: soju.cli.app.app
   :prog: soju
   :show-nested:
   :make-sections:
   :preferred: text
   :markup-mode: markdown
   :width: 78
