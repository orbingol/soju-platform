# Compose / local override example (not auto-loaded).
# Prefer: soju backend --config config/backend.yaml
# Or copy keys into ~/.config/soju/backend.yaml

llm:
  # Reach host (or compose-profile) Ollama from the backend container.
  base_url: "http://host.docker.internal:11434"
