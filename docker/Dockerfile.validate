FROM python:3.12-slim

WORKDIR /workspace

# No .git in the build context — pretend a version for setuptools-scm.
ENV SETUPTOOLS_SCM_PRETEND_VERSION=0.0.0

COPY pyproject.toml README.md LICENSE ./
COPY src ./src
COPY data ./data

RUN pip install --no-cache-dir -e .

CMD ["poe", "validate"]
