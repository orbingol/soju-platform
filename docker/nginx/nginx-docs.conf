# Serve baked Sphinx HTML on the docs container's designed internal port.
server {
    listen 14323;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
